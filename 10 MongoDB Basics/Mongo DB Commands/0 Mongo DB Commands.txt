MongoDB Useful Commands

✅ MongoDB Installation Check :::
    mongo

✅ Show Databases :::
    show dbs

✅ Create or Switch Database :::    
    use <DatabaseName>

✅ Show Collections (Tables) :::
    show collections

✅ View Current Database :::
    db

✅ Drop Database ::: 
    db.dropDatabase()

✅ Create Collection (Table) :::
Collections are created automatically when you insert the first
document, but you can also explicitly create one:
    db.createCollection("<CollectionName>")

✅ Drop Collection
    db.<CollectionName>.drop()

✅ Insert Document (Create)
👉 Insert a single document:
    db.<CollectionName>.insertOne({ name: "Riyaz", age: 25, city: "Ahemdabad" })

👉 Insert multiple documents:
    db.<CollectionName>.insertMany([
        { name: "Riyaz ", age: 25, city: "Ahemdabad" },
        { name: "Arbaz", age: 25, city: "Ahmedabad" }
    ])

✅ Read Documents (Retrieve)
👉 Get all documents:
    db.<CollectionName>.find()

👉 Get documents in pretty format (more readable):
    db.<CollectionName>.find().pretty()

👉 Get documents with a condition:
    db.<CollectionName>.find({ age: { $gt: 25 } })

👉 Get a single document:
    db.<CollectionName>.findOne({ name: "Riyaz" })

✅ Update Document
👉 Update a single document:
    db.<CollectionName>.updateOne(
        { name: "Riyaz" }, 
        { $set: { age: 31 } }
    )

👉 Update multiple documents:
    db.<CollectionName>.updateMany(
        { city: "Delhi" }, 
        { $set: { city: "New Delhi" } }
    )

✅ Delete Document
👉 Delete a single document:
    db.<CollectionName>.deleteOne({ name: "Shraddha" })

👉 Delete multiple documents:
    db.<CollectionName>.deleteMany({ city: "Pune" })

👉 Drop the entire collection:
    db.<CollectionName>.drop()

✅ Count Documents
    db.<CollectionName>.countDocuments()

👉 With condition:
    db.<CollectionName>.countDocuments({ age: { $gte: 30 } })

✅ Indexing (for performance)
👉 Create index:
    db.<CollectionName>.createIndex({ name: 1 })

👉 Show indexes:
    db.<CollectionName>.getIndexes()

✅ Aggregation Example

👉 Count grouped by city:
    db.<CollectionName>.aggregate([
        { $group: { _id: "$city", total: { $sum: 1 } } }
    ])

✅ Exit Mongo Shell
    exit

