NPM Validator Package ::
In Node.js, you can use the **validator** package to validate input **before saving** to MongoDB.

Website :: 
https://www.npmjs.com/package/validator

Commads to install :: 
npm install validator
npm i validator

Example:
const validator = require("validator");

const email = "test@gmail.com";
if (validator.isEmail(email)) {
  console.log("Valid email");
} else {
  console.log("Invalid email");
}

Use this with **Mongoose Schema Validation**.


Before ES6
var validator = require('validator');

ES6
import validator from 'validator';
import isEmail from 'validator/lib/isEmail';
import isEmail from 'validator/es/lib/isEmail';

  emailID : {
    type : String,
    required : false,
    trim : true,
    validate : {
      validator : function(v) {
        return /^\d{3}-\d{3}-\d{4}$/.test(v);     // return true if valid else false
      },
      message : props => `${props.value} is not a valid phone number! Format should be XXX-XXX-XXXX.`
    }
  },